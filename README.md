# lumen-oracle

## Backend

```bash
npm install
npm run dev
```

## Frontend (Next.js)

```bash
cd frontend
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_URL` to point at the backend API (defaults to `http://localhost:4000/api`).
